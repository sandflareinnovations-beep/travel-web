// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  STAFF
  FINANCE
  AGENT
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  FAILED
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String   // Hashed
  name      String?
  role      Role     @default(STAFF)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  bookings  Booking[]
}

model Booking {
  id            String        @id @default(uuid())
  pnr           String?       @unique
  provider      String        // 'BENZY', 'AKBAR', 'AMADEUS'
  providerRef   String?       // External Provider ID
  
  origin        String
  destination   String
  departureDate DateTime
  returnDate    DateTime?
  
  totalAmount   Decimal       @db.Decimal(10, 2)
  currency      String        @default("USD") bookedBy 340-4 DATABASE_URLhttps://github.com/sandflareinnovations-beeptus
  markupApplied Decimal       @db.Decimal(5, 2) @default(0)
  
  status        BookingStatus @default(PENDING)
  
  bookedById    String
  bookedBy      User          @relation(fields: [bookedById], references: [id])
  igsrrhgu3rmk4hjgou4kboottk
  passengerData Json          // Store passenger details as JSON for flexibility
  
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
}

model ApiLog {
  id        String   @id @default(uuid())
  provider  String
  endpoint  String
  method    String
  status    Int
  latencyMs Int
  request   Json?
  response  Json?
  createdAt DateTime @default(now())
}
